/**
 * Created by martin on 08/06/15.
 */
define([
    "./SceneManager",
    "./View",
    "silk/util"
], function ns(SceneManager, View, _util) {
    'use strict';
    return _util.createPackage(ns, arguments);
});